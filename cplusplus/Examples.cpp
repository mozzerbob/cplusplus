#include <iostream>
#include <string>

using namespace std;

int main(int argc, char const *argv[]) {

  int x = 0;
  if (x>0)
    cout << "x is greater than zero: " << x;
  else
    cout << "x is less than or equal to 0: " << x;

  cout << "\n";

  system("PAUSE");
  return 0;
}
